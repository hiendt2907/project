# Omni Gateway only — FastAPI ingress (Kafka + Redis). Không dùng chung requirements.txt với worker.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
redis[hiredis]>=5.0.0
aiokafka>=0.11.0
prometheus-client>=0.21.0
# Admin config store (Postgres omni_admin source-of-truth)
asyncpg>=0.29.0
# Catalogue lệnh chẩn đoán (config/diagnostic_commands.yaml). Loader có fallback JSON
# cho bundle agent tối giản, nhưng gateway đọc chính file YAML nên cần parser thật.
pyyaml>=6.0.0
