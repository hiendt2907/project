# AGENTS.md

> Entry point for Codex/other agent tools that read `AGENTS.md` by convention.
> **Single source of truth is [`CLAUDE.md`](CLAUDE.md)** (root of this repo) — read that file, not
> a copy. This file used to duplicate CLAUDE.md's content and drifted out of sync (still described
> retired split-role deployments as active as of 2026-07-22) — it is now a pointer only, to avoid
> that failure mode recurring.
>
> Also read `MEMORY.md` and `docs/CODEBASE.md` first, per `CLAUDE.md`'s own opening instruction.
