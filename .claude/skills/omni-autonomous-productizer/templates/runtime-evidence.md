# Runtime Evidence Template

Dùng để ghi bằng chứng runtime cho một full event cycle (xem operating-model.md).

```
Event cycle: <ví dụ: Agent discovery tick -> Twin revision update>

Step 1: <tên bước>
  Command/API:
  Output (excerpt):
  Timestamp:

Step 2: <tên bước>
  Command/API:
  Output (excerpt):
  Timestamp:

...

Conclusion:
  All arrows have evidence: yes/no
  If no, which arrow is missing evidence:
  Evidence label for this capability: VERIFIED_RUNTIME | PARTIAL | ...
```
