# Registry: trace_id ↔ scenario_id (Matrix / chaos lab)

Cập nhật **trong lúc** chạy scenario — không trì hoãn sau phiên.

| chaos_run_id | scenario_id | trace_id | learning_round | ghi_chú_thời_điểm |
|--------------|-------------|----------|----------------|-------------------|
| | | | | |
| | | | | |

**Quy định:**

- `chaos_run_id`: stamp một phiên (ví dụ `20260407-lab-01`).
- `learning_round`: `1` hoặc `2` cho Learning Delta.
- Nếu dùng Google Sheet: export CSV định kỳ vào `reports/chaos-rag-selflearn/` khi cần audit.
