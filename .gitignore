.env
evidence/
__pycache__/
*.pyc
.cursor/
.cursor.bak-*/

# MkDocs output (built site; CI uploads artifact)
wiki/site/

# Gitleaks scan artifacts
leak_report.json
leak_report_history.json

# audit cleanup 2026-06-03
chaos_drill_results_*.json
dist/

# Node / Next.js build + deps (portal UIs)
node_modules/
.next/
*.tsbuildinfo
.DS_Store
.coverage

# go-prod cleanup 2026-06-03: transient agent/tool output + build artifacts
.claude/worktrees/
.reports/
omni-remote-agent/agent

# Claude session automation state (metadata only; keep .gitkeep)
.claude/state/*.json
.claude/state/*.tmp
.claude/state/.*.tmp
!.claude/state/.gitkeep

# Playwright test artifacts
test-results/
playwright-report/

# Coverage HTML output (gate)
htmlcov-gate/

# Cloudflare — credential và config đã điền. TUYỆT ĐỐI không commit.
.cloudflared/
cloudflare/**/config.yml
cloudflare/**/*credentials*.json
cloudflare/k8s/*-config.yaml
!cloudflare/k8s/*-config.example.yaml
*.cloudflared.plist

# Cache do wrangler tự tạo khi deploy Pages — không thuộc source.
.wrangler/

# Dữ liệu khảo sát hạ tầng/bảo mật của BÊN THỨ BA (FPT Loyalty), không thuộc
# dự án Omni — tuyệt đối không commit (chứa IP nội bộ + phát hiện secret
# plaintext của hệ thống production của khách hàng khác).
/fpt-loyalty-sre-compat-report.md
/fpt_loyalty_topology.html
